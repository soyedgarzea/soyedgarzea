# Safe placeholder values only. Real .env files must not be committed.
# Add required variables here when the consuming project defines them.

APP_ENV="replace-in-consuming-project"
NEXT_PUBLIC_APP_ENV="replace-in-consuming-project"
