name: Preview PR

on:
  workflow_dispatch:
  push:
    branches:
      - develop

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: preview-pr
  cancel-in-progress: false

jobs:
  preview-pr:
    name: Preview PR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create or update preview PR
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node .github/scripts/create-preview-pr.mjs

      - name: Summary
        if: always()
        run: |
          {
            echo "## Preview PR"
            echo ""
            echo "- Result: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"
